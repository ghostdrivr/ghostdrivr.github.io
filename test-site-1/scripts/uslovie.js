let uslovie = 3 + 5;
if (uslovie === 8) { // Если значение объявленной переменной будет действительно 8, то...
    alert ('Вы чертовски правы :)')// Выйдет вот это сообщение...
} else {
    alert ('Из вас такой себе математик :(')// А если нет, то это.
}
// Если хотите узнать как это работает, то поменяейте значение 'uslovie === 8', на что-нибудь другое